interface:
  display_name: "Chess"
  short_description: "Repo-specific patterns and workflows for chess"
  default_prompt: "Use the chess repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true